# version: '1.29.0'

services:
  inance:
    container_name: inance
    image: ubuntu:22.04
    hostname: inance
    domainname: inance.com
    build:
      context: .
      dockerfile: Dockerfile      
      args: 
        PARKAGES: "nano wget curl"
    ports:
      - "6060:80"
    volumes:
      - ./website/inance-html:/var/www/html/
    networks:
      - web-network
    environment:
      - WEB_ENV=inance-apache
      - APP_VERSION=1.0

  mediplus:
    container_name: mediplus
    image: ubuntu:22.04
    hostname: mediplus
    domainname: mediplus.com
    build:
      context: .
      dockerfile: Dockerfile
      args: 
        PARKAGES: "nano wget curl"
    ports:
      - "7070:80"
    volumes:
      - ./website/mediplus-lite:/var/www/html/
    networks:
      - web-network
    environment:
      - WEB_ENV=mediplus-apache
      - APP_VERSION=1.0

  gaming:
    container_name: gaming
    image: ubuntu:22.04
    hostname: gaming
    domainname: gaming.com
    build:
      context: .
      dockerfile: Dockerfile
      args: 
        PARKAGES: "nano wget curl"
    ports:
      - "8080:80"
    volumes:
      - ./website/templatemo_589_lugx_gaming:/var/www/html/
    networks:
      - web-network
    environment:  
      - WEB_EMV=gaming-apache
      - APP_VERSION=1.0

networks:
  web-network:
    driver: bridge

        